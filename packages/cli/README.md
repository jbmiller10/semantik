# Semantik Plugin CLI

Generate and validate Semantik plugins.

## Commands

- `semantik-plugin new <name> --type <embedding|chunking|connector>`
- `semantik-plugin validate <path>`

Install via:

```bash
pip install semantik[cli]
```
